[bumpversion]
current_version = 6
parse = (?P<version>\d+)
serialize = {version}
commit = True
tag = True

[bumpversion:file:export_presets.cfg]
search = version="{current_version}"
replace = version="{new_version}"
